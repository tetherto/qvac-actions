{
    "bucketName": "just name without any path",
    "awsRegion": "eu-central-1",
    "localBasePath": "where model files will be stored after upload, f.e. q4f16_1",
    "models": {
        "model1": {
            "s3BasePath": "path to model 1 folder on S3. This folder should contain folders as dates",
            "tags": ["tag1", "tag2", "tag3"],
            "version": "model 1 version"
        },
        "model2": {
            "s3BasePath": "path to model 2 folder on S3. This folder should contain folders as dates",
            "tags": ["tag1", "tag2", "tag3"],
            "version": "model 2 version"
        }
    }
}
