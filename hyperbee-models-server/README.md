# Hyperbee models server

## Getting started

`npm install`

Fill config.json with your values.

configure AWS credentials as the env variables
AWS_ACCESS_KEY_ID
AWS_SECRET_ACCESS_KEY

run with `node index.js`