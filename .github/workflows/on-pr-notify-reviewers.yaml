name: On PR Notify Reviewers

on:
  pull_request:
    types: [opened, ready_for_review, reopened, synchronize, labeled]

jobs:
  notify-reviewers:
    uses: ./.github/workflows/automatically-add-reviewers.yaml
    with:
      reviewers: 'u011,natachikhinashvili'
    secrets: inherit
